# En desarrollo esta variable NO es necesaria.
# Vite proxea /api → localhost:3000 automáticamente (ver vite.config.ts).
#
# En producción (Vercel), configurar esta variable con la URL del backend en Render:
# VITE_API_URL=https://tu-backend.onrender.com/api
